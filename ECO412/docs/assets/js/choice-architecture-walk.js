(function(){'use strict';
const KEY='eco412_choice_architecture_walk_v5';
const fields=['setting','role','moment','options','inaction','feature','observation','facilitated','concept','interpretation','alternative','limitation','principle','dissent','reversal','mismatch','revision'];
const $=id=>document.getElementById(id);let record=null;
function esc(v){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function read(){const d={};fields.forEach(f=>d[f]=($('caw-'+f)?.value||'').trim());return d}
function message(t,ok=false){const n=$('caw-message');if(!n)return;n.hidden=false;n.className='caw-message'+(ok?' ok':'');n.textContent=t}
function build(){const d=read();if(fields.some(f=>!d[f]))return message('Complete every observation and Adaptive Species field before building the working note.');record=d;localStorage.setItem(KEY,JSON.stringify(d));const p=$('caw-preview');p.hidden=false;p.innerHTML=`<h3>Working field note</h3><p><strong>Setting and decision:</strong> ${esc(d.setting)}. The decision-maker role is ${esc(d.role)}. ${esc(d.moment)} Actions: ${esc(d.options)} Inaction: ${esc(d.inaction)}</p><p><strong>Observable architecture:</strong> ${esc(d.feature)}. ${esc(d.observation)}</p><p><strong>Behavior facilitated:</strong> ${esc(d.facilitated)}</p><p><strong>Interpretation:</strong> ${esc(d.concept)}. ${esc(d.interpretation)}</p><p><strong>Alternative:</strong> ${esc(d.alternative)}</p><p><strong>Limitation:</strong> ${esc(d.limitation)}</p><p><strong>Adaptive Species:</strong> ${esc(d.principle)} ${esc(d.dissent)} ${esc(d.reversal)} ${esc(d.mismatch)} ${esc(d.revision)}</p>`;message('Saved locally. Review and export both files for the Model Diagnostic Case Study.',true)}
function current(){if(record)return record;try{return JSON.parse(localStorage.getItem(KEY)||'null')}catch(e){return null}}
function dl(n,t,ty){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([t],{type:ty}));a.download=n;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),300)}
function exportJSON(){const d=current();if(!d)return message('Build the working field note before exporting.');dl('eco412-module1-choice-architecture-record.json',JSON.stringify({course:'ECO 412',module:1,activity:'Choice Architecture Walk',schemaVersion:'5.0',submissionRole:'Supporting evidence for the Model Diagnostic Case Study',exportedAt:new Date().toISOString(),payload:d},null,2),'application/json')}
function exportMarkdown(){const d=current();if(!d)return message('Build the working field note before exporting.');dl('eco412-module1-choice-architecture-working-note.md',`# Choice Architecture Walk — Working Note\n\n## Setting and decision\n${d.setting}. Role: ${d.role}. ${d.moment}\n\n## Actions and inaction\n${d.options}\n\nInaction: ${d.inaction}\n\n## Observable architecture\n${d.feature}. ${d.observation}\n\n## Behavior facilitated\n${d.facilitated}\n\n## Behavioral interpretation\n${d.concept}. ${d.interpretation}\n\n## Alternative explanation\n${d.alternative}\n\n## Evidence limitation\n${d.limitation}\n\n## Adaptive Species Lens\n- Principle: ${d.principle}\n- Dissent: ${d.dissent}\n- Reversal: ${d.reversal}\n- Mismatch: ${d.mismatch}\n- Revision: ${d.revision}\n`,'text/markdown')}
function restore(){let d;try{d=JSON.parse(localStorage.getItem(KEY)||'null')}catch(e){}if(!d)return;fields.forEach(f=>{const n=$('caw-'+f);if(n)n.value=d[f]||''});record=d}
window.ChoiceArchitectureWalk={build,exportJSON,exportMarkdown};document.addEventListener('DOMContentLoaded',restore);if(typeof document$!=='undefined'&&document$?.subscribe)document$.subscribe(restore)
})();
